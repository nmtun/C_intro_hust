#include <stdio.h>

struct temp{
    int sum;
    int max_t;
    int min_t;
    int t_tb;
}thoi_tiet[12];
void cal(struct temp t_tb[]){
    for (int i = 0; i < 12; i++)
    {
        scanf("%d %d %d %d",&thoi_tiet[i].sum,&thoi_tiet[i].max_t,&thoi_tiet[i].min_t,&thoi_tiet[i].t_tb);
        if()
    }
    
}